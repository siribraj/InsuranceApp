import { WalletTopupRequest } from './wallet-topup-request';

describe('WalletTopupRequest', () => {
  it('should create an instance', () => {
    expect(new WalletTopupRequest()).toBeTruthy();
  });
});
