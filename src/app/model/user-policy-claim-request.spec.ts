import { UserPolicyClaimRequest } from './user-policy-claim-request';

describe('UserPolicyClaimRequest', () => {
  it('should create an instance', () => {
    expect(new UserPolicyClaimRequest()).toBeTruthy();
  });
});
