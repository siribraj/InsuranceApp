import { PolicyPurchaseRequest } from './policy-purchase-request';

describe('PolicyPurchaseRequest', () => {
  it('should create an instance', () => {
    expect(new PolicyPurchaseRequest()).toBeTruthy();
  });
});
